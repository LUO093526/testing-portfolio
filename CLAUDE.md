# Claude 自动技能触发规则

当用户消息中出现以下关键词/场景时，**必须先调用 Skill 工具**，再给出回答：

---

## 🔍 分析类
| 触发词 | 技能 | 示例 |
|-------|------|------|
| 「为什么出错」「根因」「bug 原因」| `5whys` | 「这个测试为什么挂了」|
| 「复盘」「事故」「故障分析」| `postmortem` | 「复盘昨天的线上故障」|

## 🎯 决策类
| 触发词 | 技能 | 示例 |
|-------|------|------|
| 「怎么选」「方案对比」「决策」| `ooda` | 「A 方案和 B 方案怎么选」|
| 「优先级」「先测哪个」「P0/P1」| `moscow` | 「这些用例哪些先跑」|
| 「为什么这样设计」「深层假设」| `socratic` | 「这个需求合理吗」|

## ⚠️ 风险类
| 触发词 | 技能 | 示例 |
|-------|------|------|
| 「上线前」「发布风险」「可能出什么错」| `premortem` | 「上线前帮我检查风险」|
| 「攻击」「安全漏洞」「怎么被绕过」| `redteam` | 「这个接口有什么安全漏洞」|

## 📈 改进类
| 触发词 | 技能 | 示例 |
|-------|------|------|
| 「回顾」「总结这周」「改进点」「Sprint」| `retro` | 「回顾这周的学习」|

## 🧬 自我进化
| 触发词 | 技能 | 示例 |
|-------|------|------|
| 「进化」「升级」「自我改进」「能力评估」| `self-evolve` | 「检查一下有没有新能力可以学」|

---

## 场景模式识别（不需要精确匹配关键词）

- 用户描述**反复出现的问题** → `5whys`
- 用户**即将做重大操作**（上线/发布/大重构）→ `premortem`
- 用户遇到**线上事故/测试挂了需要排查** → `postmortem`
- 用户在**多个选项间犹豫** → `ooda`
- 用户列了**一堆待办不知道先做哪个** → `moscow`
- 用户在 **review 代码/设计/方案** → `redteam`
- 用户在**反思/问自己做得怎么样/周总结** → `retro`
- 用户**质疑需求/追问深层原因/挑战假设** → `socratic`

---

## 触发规则

1. **先调技能，再回答** — 匹配到条件时 Skill 工具调用必须在普通文字之前
2. **一问一技能** — 每次只触发最匹配的一个
3. **不明显时不触发** — 把握不准就正常回答，宁可漏触发不要滥触发
4. **技能不存在时回退** — 用自然语言模拟该框架（如「我用五问法分析…」）
